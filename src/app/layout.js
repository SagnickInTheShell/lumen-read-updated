import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Lumen Read — Adaptive Reading System",
  description: "An intelligent reading system that improves accessibility and dynamically adjusts content based on your reading behavior. Features dyslexia mode, focus mode, text-to-speech, voice control, and eye tracking.",
  keywords: "reading, accessibility, dyslexia, ADHD, focus, text-to-speech, adaptive",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
